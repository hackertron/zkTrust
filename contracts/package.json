{
  "name": "zktrust-starknet-contracts",
  "version": "1.0.0",
  "description": "Starknet contracts for ZKTrust platform",
  "main": "deploy.js",
  "scripts": {
    "test": "node test_deployment.js",
    "deploy": "node deploy.js",
    "compile": "cd zktrust_verifier && cairo-compile verifier.cairo --output target/release/zktrust_verifier.json"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "starknet": "^5.19.5"
  }
}
